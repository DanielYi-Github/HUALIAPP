// @flow
import variable from "../../variables/platform";

export default (variables /*: * */ = variable) => {
  const loginPageTheme = {
	LoginPageIcon:variables.LoginPageIcon,
	PageSize:variables.PageSize
  };

  return loginPageTheme;
};
