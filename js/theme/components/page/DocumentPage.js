// @flow
import variable from "../../variables/platform";

export default (variables /*: * */ = variable) => {
  const documentPageTheme = {
	ExplainText:variables.ExplainText,
	SpinnerColor:variables.SpinnerColor,
  };

  return documentPageTheme;
};
